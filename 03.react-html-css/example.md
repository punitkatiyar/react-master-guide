```
function App() {
  let title={
    color:'tomato',
    fontSize:'4rem'
  }
  return (
    <>
    <h1 style={title}>Welcome to React Master Guide</h1>
    <hr></hr>
    <p>JSX (JavaScript XML) is a syntax extension used in React to describe the structure of user interface components in a more concise and intuitive way. It allows you to write HTML-like code directly in your JavaScript files, which is then transformed into JavaScript objects by the transpiler (such as Babel) before it's run by the browser. JSX (JavaScript XML) is a syntax extension used in React to describe the structure of user interface components in a more concise and intuitive way. It allows you to write HTML-like code directly in your JavaScript files, which is then transformed into JavaScript objects by the transpiler (such as Babel) before it's run by the browser.</p>
    <hr></hr>
    <img src="./image/punitkatiyar.jpg" width={"100%"} height="700px" alt="banner"/>
    <hr></hr>
    <center>
    <a  href="./">Home</a>&nbsp;&nbsp;&nbsp;&nbsp; || &nbsp;&nbsp;&nbsp;&nbsp;
    <a  href="./">Home</a>&nbsp;&nbsp;&nbsp;&nbsp; || &nbsp;&nbsp;&nbsp;&nbsp;
    <a  href="./">Home</a>&nbsp;&nbsp;&nbsp;&nbsp; || &nbsp;&nbsp;&nbsp;&nbsp;
    <a  href="./">Home</a>&nbsp;&nbsp;&nbsp;&nbsp; || &nbsp;&nbsp;&nbsp;&nbsp;
    <a  href="./">Home</a>&nbsp;&nbsp;&nbsp;&nbsp; || &nbsp;&nbsp;&nbsp;&nbsp;
    </center>
    <hr></hr>
    <table border={1} cellPadding={20} bgcolor="gold">
      <tr>
        <td>
          <h2>Card One</h2>
        </td>
      </tr>
    </table>

    
    </>
  );
}

export default App;
```
